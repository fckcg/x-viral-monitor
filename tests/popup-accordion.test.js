// (Empty placeholder — replaced by tests/popup-dashboard.test.js after
// the popup pivoted from Accordion (info-arch C) to Tier-Card
// Dashboard (mock D) per user feedback 2026-05-19. Kept as an empty
// describe so vitest discovery doesn't get confused.)

import { describe, it } from 'vitest';

describe('#45 popup accordion (deprecated)', () => {
  it('replaced by tests/popup-dashboard.test.js', () => {});
});
